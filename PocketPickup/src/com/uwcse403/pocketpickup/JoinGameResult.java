package com.uwcse403.pocketpickup;

public enum JoinGameResult { 
	ERROR_JOINING, ALREADY_ATTENDING, SUCCESS
}
/*
public enum JoinResult{
	ERROR_JOINING(0), ALREADY_ATTENDING(1), SUCCESS(2);
	private int val;
	private JoinResult(int val) {
		this.val = val;
	}
}
*/	
